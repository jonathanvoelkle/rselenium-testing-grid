name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: r-lib/actions/setup-r@v2
      - uses: r-lib/actions/setup-r@v2
      - name: install `devtools`
        run: |
          R -e 'install.packages("remotes")'
      - name: install `RSelenium`
        run: |
          R -e 'remotes::install_github("ropensci/RSelenium")'
